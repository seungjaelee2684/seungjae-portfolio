import React from 'react'
import styled from 'styled-components';
import { InBoxContainer } from '../../pages/AboutMePage';

const MobileView = () => {
  return (
    <InBoxContainer>
        MobileView
    </InBoxContainer>
  )
};

export default MobileView;